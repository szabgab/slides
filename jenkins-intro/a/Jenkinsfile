pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'build'
                pwd()
                sh 'id'
                sh 'uname -a'
            }
        }
    }
}
