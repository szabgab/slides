
while True:
   line = get_next_line()
   
   if last_line:
       break
   
   if line is empty:
      continue

   if line has an # at the  beginning:
      continue

   if line has a // at the beginning:
      continue

   do the real stuff
