# JavaScript code

```markdown
{{#include ../examples/js.md}}
```

* One can include plain HTML in the md file. So include `script` tags.
* The code should come at the end to have the DOM available already.


