# Write to file

* write

* Write the content to the file
* Raise exception if cannot open file for writing
* e.g. raise `File::NotFoundError` if parent directory does not exist

{% embed include file="src/examples/files/write_to_file.cr" %}


