# How to install 3rd party shards?

* Create a file called `shard.yml` for your project listing the dependencies of this project
* Run `shards install`

{% embed include file="src/examples/shard.yml" %}


