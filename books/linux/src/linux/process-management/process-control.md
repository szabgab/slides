# Process control

* kill

```
$ kill       SIGTERM   (same as kill -15)  = please die
$ kill -9    SIGIKILL                      = die now!
$ kill -l    list available signals

$ man 7 signal
```



