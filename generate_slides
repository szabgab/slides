rm -rf html
if [ "$1" != "" ]
then
   names=$1
else
   names=$(cat slides.txt)
fi

# echo $names
# echo '-----'

for name in $names
do
    echo $name
    python ../slider-py/slider.py --md $name/intro.md --html --dir html/$name/ --templates templates/ --static static/ || exit 1
done
